# 結算修正與策略檢討

## 已修正：結算動畫提早入帳

2026-09-20 影片 337.8–339.6 秒：同一筆最後顯示 400 的獎勵，逐幀 OCR 曾讀到 60、280、40、400。原程式首次讀到正數即入帳，影片實際記錄 20，帳目從 360 增至 380。

現在至少等待 1.5 秒、同值至少 3 次且維持 0.6 秒，並且金額必須屬於遊戲獎勵倍數。若收手詢問畫面的金額已確認，必須與結算金額一致。確認前不入帳、不點擊下一局；8 秒仍无法確認則清楚報錯停止，避免默默漏記。已入帳標記直到下一局才重設，UNKNOWN 閃動不會重複記帳。

用該段影片的實際影格、OCR、狀態辨識與圖示匹配重播，僅攔截滑鼠及磁碟帳目寫入。兩種策略均只寫入一次 `(760, 0)`，即原 360 加上完整 400。沒有自動校正過去的錯誤帳目，因為無法由這一筆推算全天漏記金額。

驗證：12 項測試通過，包括這次私有影片重播；先前的另一組私有截圖測試因素材缺失而跳過，未冒稱重新通過。設定 HOLOLIVE_TEST_VIDEO 為 2026-09-20 影片路徑後執行 `python -m unittest discover -s tests -v` 可重現。

## 已檢討、尚未修改：時間策略

重建片末的牌堆（起手 2、J、6、6 加一張 Joker；翻倍明牌 6；預判 A），已入帳 360、手上 400 的結果為收手 509.93 秒、繼續 587.15 秒。模型中的「翻到 800 後立即收手」本身被估為 611.85 秒，比目前 400 收手還高。不是漏掉「翻成功後仍能收手」這個選項；現有前瞻已包含該選項，錯誤／不合需求的估值來自它共用的後續時間表。

相同牌堆、相同 400 與 A，將帳目從 380 改為 400，收手估值會從 509.93 跳到 638.47 秒，動作從收手變繼續。將數值求解迭代上限從 80 暫時提高到 2000（僅記憶體診斷）不改變結果，所以沒有證據認為只增加迭代次數便能修復。

風險來源：

- 主模型試圖保留 19,800–19,999 的狹窄區間，固定獎勵倍數加上格距，使剩餘時間表不平滑。更接近目標可能被估為更難湊數。這種上限約束可使真實最優值不單調，不能把每一次不單調都視為數學錯誤；但目前粗略模型不應憑此大幅否決早期的安全收益。
- 主模型用固定新牌堆的轉移機率，現場用剩餘牌堆做兩步前瞻，之後又切回主模型；估值不是同一個完整狀態模型的精確解。
- 超大起手獎勵在主模型中近似成失敗重開，但現場行為是提前衝刺；評分又把跨過 20,000 的情況計作達標。模型、行為與評分目標不完全一致。
- 更早的模擬沒有包含此次入帳動畫錯誤；整體平均較快不能保證實際策略或個別決策正確。

建議的下一版設計（本次尚未實作）：

1. 將早期累積與接近上限的補差額分開，避免早期 400 的價值被最末端湊數的模型波動主導。
2. 建立明確的安全翻倍規則：未達標、翻後不跨上限，且牌堆確有可贏的牌、沒有會輸的牌時，優先繼續。用實際牌數判斷，不讀四捨五入的「100%」。這是可解釋的策略約束，不宣稱是所有耗時／上限條件下的全域最優定理。
3. 在剩餘時間估值之外檢查收益與時間的基本關係；估值嚴重矛盾時使用經離線驗證的簡單規則，而不是盲目相信較小的秒數。僅把時間表強制排成單調亦不能證明已找到最佳策略。
4. 用「360／380、手上 400、A／2」以及多組安全金額作回歸案例；另測達標可結算、翻後跨上限、只有平手牌、未知牌與辨識失敗，確保規則沒有破壞邊界處理。
5. 離線比較固定／分段獎金目標加牌面規則，保留失敗重開、平手、結算時間，分開報告達標且保留最後一局與提早跨上限。先定規則，再做實機驗證，不因單一平均值較好就發布。
