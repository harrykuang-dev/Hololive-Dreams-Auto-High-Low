# v1.2.0-beta.2 策略設計

狀態：設計完成，策略尚未實作。基線為 beta 分支的入帳與中文路徑修正 dadb238。本文的門檻為待比較的候選參數，沒有宣稱已找到最快策略。

## 目標與統一計分

第一階段：盡快把「已確認入帳的當日累計獎勵」推進至 19,800–19,999，保留下一局。不是扣除門票後的淨利，也不是把本局尚未結算的獎勵加進帳目。

第二階段：達標後持續挑戰遊戲允許的翻倍次數。由遊戲實際狀態確認強制結算／上限，不把模擬器目前假設的 10 次當作遊戲規則。

直接入帳至 20,000 以上列為「提前跨上限」，不能當作第一階段的成功。超時、辨識停止、牌堆不可信及未達標均單列；只報成功樣本的平均時間會有偏差，必須同時公布全部結果比例。

## 決策輸入與責任

- `coins`：SettlementReader 確認後的帳目；`cash`：目前可領獎勵；`next_cash`：下一次成功的獎勵。必須核對 `next_cash == 2 * cash`，不把 OCR 的第一個合法數字直接當作已確認狀態。
- `rank`、剩餘牌數與可信狀態：適配器只在副本扣除預判牌一次，原記牌流程維持單一所有者。相同畫面重讀不能再次扣牌。
- `wins / losses / ties`：依實際選擇的高／低方向統計。條件勝率為 `wins / (wins + losses)`；平手另外計算時間，不能當成功。
- 已確認帳目、獎金、牌堆任一不可信時，不得套用「必勝」規則。未知第一張牌是正常遊戲狀態，與 OCR 失敗分開處理。
- 策略只回傳動作及原因；不點擊、不入帳、不改牌堆。執行層保留 beta 修正後的流程。

## 規則優先序

以下由上而下執行，第一條命中即返回；時間估值沒有覆寫這些規則的權限。

| 優先 | 條件 | 動作／原因 |
|---|---|---|
| 1 | 必要狀態矛盾或金額未確認 | 重讀，達等待期限停止並保留現場；不編造金額或無限卡住 |
| 2 | 遊戲已強制結算／沒有挑戰選項 | 交回結算流程 |
| 3 | 已入帳至少 19,800、仍可遊玩 | 進入衝刺，繼續挑戰，不設軟體止盈次數 |
| 4 | `19,800 <= coins + cash < 20,000` | 收手，確認入帳後才切換衝刺 |
| 5 | `coins + cash >= 20,000` | 提前衝刺，獨立記錄；不能假設可丟掉獎勵免費重開 |
| 6 | `coins + cash < 19,800` 且 `coins + next_cash >= 20,000` | 收手保留下一局；上限保護優先於必勝 |
| 7 | 已知可信牌堆、`wins > 0`、`losses == 0`，且翻倍後低於 20,000 | 繼續，原因 `safe_progress`；A、2 及其他無輸牌的牌面皆適用 |
| 8 | 只剩平手牌、牌堆空或計數矛盾 | 重讀核對，仍異常則停止；不能宣稱 100% 或無限重試 |
| 9 | 其餘可決策狀態 | 使用離線選定的分段規則表 |

這是符合使用者需求的策略約束，並非「必勝翻倍在所有時間與上限條件下都全域最優」的數學定理。已能達標時優先收手；平手仍會消耗時間。

## 分段規則表

現場不再求解「還要多少秒達到狹窄區間」的 Bellman 表。離線比較可解釋的策略，選定後以版本化參數固定；沒有經驗證的表則不啟用新模式，不回退到目前有問題的時間估值。

候選表用四個參數描述：累積期門檻 `H`、補差額切換餘額 `G`、累積期最低勝率 `P1`、補差額期最低勝率 `P2`。初始比較網格：

- `H ∈ {1,600, 3,200, 6,400, 12,800, 19,800}`。
- `G ∈ {1,600, 3,200, 6,400}`，剩餘 `19,800 - coins <= G` 時進入補差額期。
- `P1, P2 ∈ {0.55, 0.60, 0.65, 0.70, 0.75}`。

規則 1–8 先套用。累積期：`cash < H` 且條件勝率至少 `P1` 則繼續，否則收手。補差額期：`cash < 19,800 - coins` 且條件勝率至少 `P2` 則繼續，否則收手。翻過上限仍由規則 6 攔截。安全翻倍可以越過 H，因此不會因已到固定門檻而放棄確定且安全的收益。

正常隱藏第一張牌：用已確認牌堆枚舉第一張及下一張，計算揭牌後採最佳方向的非平手條件勝率；按剩餘牌數加權，平手另計耗時。使用同一表的 P1/P2，不視作 100%。若現場可見但辨識不到，不能偽裝成正常隱藏狀態。

19,800 候選不等於一定能從任一起手獎勵翻到 19,800；倍數序列與上限會限制可達值。6,400／12,800／直接衝目標究竟誰快，由一致的計分與實測決定。

## 時間校準與離線選型

現有影片估計：中獎開局 11.5 秒、未中獎開局 9 秒、失敗回到下注 3.125 秒、成功翻倍 4.7 秒、收手到下次下注 7.75 秒、平手額外約 2.5 秒。樣本少，僅作初始值；新的結算等待會影響收手成本，不能直接把舊模擬秒數當成修正後的實測。

新增結構化事件：單調時鐘、局號、觀測狀態、已確認帳目、在手／下一次獎金、牌面、win/loss/tie 計數、動作、原因、點擊是否成功、結算確認。以遊戲實際進入下一個狀態界定耗時，不能把重試點擊重複計作翻倍。開局、中獎／不中獎、成功／失敗／平手、結算及人工暫停分開。

比較組：原版 Legacy、現有時間模型、僅增加安全規則的時間模型、固定門檻、分段表。前三者作對照，不作預設回退。全部使用相同入帳修正、狀態資訊與耗時計算。

模擬器先修正：提前跨上限不能算合格成功；大獎不能近似成免費失敗重開；正常隱藏牌、預判牌與剩餘牌模型一致；平手消耗牌與時間；假設的強制結算次數需明列並做敏感性測試。未核實的牌堆規則必須標注，不能以精確秒數掩蓋。

選型程序：

1. 用 1,000 個固定訓練種子篩選參數，保留前 10 組；先按合格達標率，再看時間，不獎勵提早跨上限。
2. 用另外 10,000 個從未參與選型的種子驗證。按天／局共享起手與牌序；以配對 bootstrap 報告耗時差的 95% 區間。同時列出兩邊都合格的時間差、各自全部合格樣本時間和所有失敗比例，避免篩樣本美化。
3. 報告平均、中位數、P90、局數、翻倍數、合格率、跨上限率、未完成率。耗時分項上下浮動 25% 再比較，不能只針對單一時間組合調參。
4. 發行門檻：相對目前時間模型的合格率差，其 95% 區間下界不低於 -0.5 個百分點；相同合格天的平均耗時差區間上界小於 0；P90 不惡化超過 5%。不符合則仍可做實驗模式，但不能稱為更快或取代預設。

這些是預先設定的工程驗收門檻，不是遊戲的客觀定律。若沒有候選通過，記錄結果並保留原版預設，不能硬挑一個稱為最佳。

## 必須通過的回歸

| 案例 | 必須結果 |
|---|---|
| coins=360／380，cash=400，A 或 2，可信牌堆有贏牌無輸牌 | challenge / safe_progress |
| 多組未達標金額、cash 翻後低於上限、零輸牌 | 安全規則不受門檻或估值覆寫 |
| 顯示 100% 但剩餘牌仍有輸牌 | 不能命中 safe_progress |
| coins=19,400，cash=400 | 收手入帳達 19,800 |
| coins=19,000，cash=700，next=1,400 | 即使 A 也收手保留下一局 |
| coins=19,700，cash=200 | 收手入帳 19,900，下一局衝刺 |
| coins=19,800，合法在手獎金 | 挑戰，不套用累積門檻 |
| coins=19,600，cash=400 | 提前衝刺、單列異常路徑，不算合格達標 |
| 全平手、未知牌、Joker、牌堆空、重複預判影格 | 不虛構必勝；單次扣牌；可控重讀或清楚停止 |
| 結算動畫 20/60/280/40/400、UNKNOWN 閃動 | 只確認 400 一次 |
| 收手後點擊未生效／金額不符 | 不提前入帳或切換階段 |

測試除了個案，還枚舉合法獎勵、帳目邊界與不同剩餘牌堆，檢查規則優先序。舊版策略保留可選；共用的是辨識與入帳修正。

## 實作與發行順序

1. 加入不改變行為的事件紀錄，重新量測修正後的結算成本。
2. 新增獨立規則策略與上述測試；保留 Legacy，現有模型標為實驗對照。
3. 修正模擬器計分與狀態模型，完成候選參數選型，將參數、種子、假設與完整結果一起保存。
4. 影片重播須通過；再實機確認下注→留牌→挑戰→成功／平手／失敗→結算→下一局。確認遊戲收到點擊，而不只是程式發出點擊。
5. 至少完成一次真實達標並進入衝刺的流程，帳目逐筆核對；上限尚未觀測到時明確列為未驗證，禁止以模擬上限冒充實測。
6. 發佈新的 beta 與其獨立標籤，不覆寫 beta.1 的既有標籤／二進位。README 各語言同步模式說明與限制；穩定版預設仍保留 Legacy，直到取得更多實機證據。

目前交付的是可實作與驗收的設計；規則策略、事件紀錄、模擬器改造及實機驗證均屬下一輪實作，沒有在本次提交中暗中啟用。
