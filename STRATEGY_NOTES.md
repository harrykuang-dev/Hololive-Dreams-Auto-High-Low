# 實測校準更新（2026-09-18，以本節為準）

使用者已確認收益記錄正確，本次不校正或改寫實際帳目。

新版使用無獎勵重開週期 9.920 秒、成功翻倍週期 4.245 秒。模型將失敗收尾另算 2 秒，因此 round_seconds 設為 7.920，兩者相加才是實測的 9.920，避免重複計算。收取獎勵與失敗收尾各 2 秒仍是估計；中獎動畫與平手耗時樣本不足。

取消草稿強制至少收取 800 的門檻，min_cashout 預設為 0：依勝率、在手獎金、距離 19,800 的差額及重開成本，動態比較收取與繼續翻倍的預估達標時間。達到 19,800 後取消程式的止盈門檻，持續挑戰至遊戲上限或使用者停止；不繞過遊戲本身限制。

同種子 200 天模擬比較最低收取門檻 0、400、800、1,600，平均耗時分別 454.2、458.9、471.0、472.0 秒，因此選 0。再用獨立種子 876541 驗證 1,000 天：舊策略平均 644.1 秒、50.600 局；新版平均 453.8 秒、34.147 局，模型內節省約 29.5% 時間、32.5% 局數。達標後仍低於 20,000 分別為 805/1000、1000/1000 天。

上述是實測時間校準的模擬，不是新版實機效果；仍假設固定時間、52 張獨立翻倍牌堆、同點重抽、虛構 10 次成功後結算、沒有辨識錯誤。詳見 benchmark_measured_timing.json、benchmark_calibrated_validation.json。可用 `../.venv/Scripts/python.exe benchmark_strategy.py --days 1000 --seed 876541 --output benchmark_calibrated_validation.json` 重現。

啟動 dist/Hololive Dreams-Auto/Hololive Dreams-Auto.exe，可切換新版與舊版。原版 EXE 未覆寫。新目錄啟動須複製原 daily_coins.json 或填入正確的當日已結算收益；兩個程式不要同時掛機。

---

# 先前草稿紀錄（下列時間、800 門檻與模擬數字已由上節取代）

目標：盡快累積已結算獎勵至 **19,800–19,999**，保留每日 20,000 上限前的最後一局，再持續挑戰翻倍，直到遊戲強制結算、顯示上限或玩家停止。

## 啟動及切換

- 新執行檔：`dist/Hololive Dreams-Auto/Hololive Dreams-Auto.exe`。請先停止並關閉舊程式，以免兩個程式同時點擊遊戲。
- 下拉選單可選「時間優先 → 最大翻倍」或「舊版 60% → 獎金 10,000」。必須停止掛機後切換。
- 「校正今日」填入今日已結算獎勵總額，**不是錢包餘額，也不是扣門票後淨利**。新舊策略共用同一份每日帳目，切換不會歸零。
- 原本資料夾的 EXE 及正在使用的 v1.0.1 EXE 均未覆寫。`legacy/` 保存上游原始策略及 UI 供查閱；完整原始版本可由 Git 的 `286fabe58024bd48ddb79fc344f69d7735c760bd` 還原。
- 來源碼執行：在本資料夾使用 `../.venv/Scripts/python.exe main_ui.py`。

## 策略差異

| 條件 | 舊版 | 新版 |
|---|---|---|
| 小獎金、下一張約五成勝率 | 通常收手重開 | 在手低於 800 先繼續挑戰，減少收小獎後重開 |
| 一般累積階段 | 固定 60% 門檻 | 比較收手與繼續的預估剩餘耗時 |
| 未知下一張牌 | 當作 100% | 對未知牌分布取平均 |
| 目前收手可到 19,800–19,999 | 部分情況仍會冒險 | 立即結算，下一局衝刺 |
| 達標後 | 在手 ≥10,000 就收手 | 不設獎金或翻倍次數止盈門檻 |
| 一開始的獎金已會跨過 20,000 | 強行翻倍 | 提前衝刺，記錄 `oversized_hand_early_sprint` |

800 是可調的小獎金門檻：200 起手通常至少成功翻兩次才考慮收手。接近每日上限時，以保留最後一局為優先。此門檻表達「多翻少重開」的偏好，**不代表 800 已被證明是全域最佳值**。

時間模型用 Bellman 更新估算達標時間，以 100 代幣為步長、失敗後重開為回返狀態。開局看不到點數時不提供免費預知；實際決策再用已扣除明牌的牌堆向前看兩步。高／低方向仍選剩餘牌堆中勝出機率較大的方向。留牌演算法未改，讓比較聚焦翻倍策略。

## 模擬比較（非遊戲實測）

可執行 `../.venv/Scripts/python.exe benchmark_strategy.py --days 1000 --seed 531987` 重現主要比較。

| 指標 | 舊版 | 新版 |
|---|---:|---:|
| 平均到達 19,800 的時間 | 788.96 秒 | 560.36 秒 |
| 平均遊玩局數 | 50.39 | 34.72 |
| 中位時間 | 722.50 秒 | 502.75 秒 |
| 第 90 百分位時間 | 1,301.50 秒 | 941.50 秒 |
| 達標後仍可開最後一局 | 781/1000 日 | 980/1000 日 |

此模型下平均耗時降低 **29.0%**、局數降低 **31.1%**。同一模擬日／局共用同樣起手獎金與洗牌序列；配對時間差約為 228.60 秒，近似 95% 信賴區間為 208.02–249.18 秒。跨過每日上限的日子也列入耗時，另行統計最後一局可用率，沒有隱藏失敗樣本。

把重開耗時改為 6 秒及 24 秒，各自再模擬 1,000 日，平均耗時分別減少約 25.9% 及 30.5%。原始逐日結果見 `benchmark_results.json`、`benchmark_fast_rounds.json`、`benchmark_slow_rounds.json`。

**限制與假設：**

- 起手獎金先驗由固定種子抽取 128 組五張牌，再用原留牌引擎精確枚舉該手換牌結果求平均。不是實際遊戲獎金紀錄，也不是枚舉全部起手牌。
- 模擬假設猜高低重新使用 52 張普通牌，同點消耗一张牌後重猜，第一張未知、後續點數可見；實際 bot 則扣除本局已觀察的牌。遊戲是否重新洗牌仍須確認。
- 模擬假設成功翻倍 10 次強制結算；這是**模擬上限**，不是已確認的遊戲規則。實際新版沒有這個 10 次上限。
- 基準時間：開局／留牌 12 秒、每次猜牌 2.5 秒、結算及失敗處理各 2 秒。未將 OCR 錯誤、牌面辨識和硬體效能差异視為已校準。
- 數值模型有離散化、有限前瞻與罕見大獎近似，因此沒有保證真實遊戲中的絕對最短時間。
- 猜高低出現百搭王會暫停，因提供的規則只說明撲克牌型中的百搭王，沒有證實翻倍階段如何處理。
- 尚未完成新版實際遊戲端到端試跑。觀察期間電腦操作被 Escape 中止，後續只做程式及離線測試。

## 設定與記錄

EXE 同目錄的 `strategy_config.json` 可調 `min_cashout`、各階段秒數與 `day_reset_hour`。預設按電腦當地時間 00:00 換日，**不宣稱這就是伺服器換日時間**；可改成遊戲實際換日對應的本地小時。修改後重啟掛機。

- `daily_coins.json`：已结算累計、失敗次數、觀察到的本日局數、最大成功翻倍次數。舊帳目可沿用。
- `strategy_events.jsonl`：本地決策、當時牌堆、兩種策略各自的答案、局數、狀態轉換、結算與達標耗時。從非零累計開始，`elapsed_seconds` 只代表該次工作階段所用時間。
- 舊 UI 的「淨利」欄維持 `累計獎勵 − 失敗次數×50` 的舊公式，**不是扣除所有局數入場費的真實淨利**，不參與達標判斷。
- 未辨識獎金不會擅填 200；連續兩次讀到相同正數才決策。結算數字與要求收手的金額不一致會暫停。日誌仍需用遊戲畫面核對，兩次相同 OCR 不是正確性的保證。
- 每局每個卡片 ID 只扣一次；結算／失敗鎖定到下一局，短暫 UNKNOWN 不會重複入帳。接管已存在的結算畫面不會再次加錢。
- 45 秒畫面未推進或 30 秒 UNKNOWN 會暫停。停止快捷鍵保留 INSERT。

## 測試與建置

```
../.venv/Scripts/python.exe -m unittest discover -s tests -v
../.venv/Scripts/python.exe -m PyInstaller --noconfirm "Hololive Dreams-Auto.spec"
```

18 個單元及無畫面整合測試涵蓋舊版分支一致性、達標收手、取消 10,000 止盈、小獎金續翻、牌堆方向、OCR 穩定、換日、損壞帳目拒絕載入、結算閃爍去重與失敗點擊不入帳。

上游：[自動猜高低](https://github.com/harrykuang-dev/Hololive-Dreams-Auto-High-Low)；撲克規則：[hololive-dreams-helper](https://github.com/Oreki0504/hololive-dreams-helper/blob/main/README.zh-CN.md)。後者確認 52 普通牌＋1 百搭王、門票 50、最低兩對及棄牌不放回；未記載翻倍次數上限、同點規則或每日上限刷新時間。
