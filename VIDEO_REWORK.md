# 影片驗證後的時間策略重作

基礎版本：1.0.1（目前含 README 翻譯同步提交 f3f7016）。本次保留 1.0.1 的下注、留牌、點擊、翻牌追蹤及收益紀錄流程，只在翻倍詢問處插入可選策略。預設「1.0.1 原版 / Legacy」，可在停止時切換「時間優先 / Time priority」。兩者共用修正後的獎金裁切。

## 1.1 卡住的原因與重現

使用者提供約 499.75 秒、60 FPS 的影片。100 秒畫面為 1.1 舊策略，卡在下一次獎勵 800 的挑戰詢問；125 秒為 1,600，150 秒已換成 1.0.1，畫面為 3,200。

舊黃色文字辨識框同時包含獎金數字及下方錢包硬幣的一小部分，整框裁切把兩者一起送入 OCR，離線重現讀成 0、8、9 等錯值。1.1 共用迴圈要求連續兩次正數一致，另拒絕奇數，因此反覆等待／報錯；這發生在選擇策略之前，兩種策略都受影響。1.0.1 讀不到時用預設值繼續，故能運作，但預設值不能拿來做可靠的時間決策。

修正：按連通區與同一行的數字裁切，排除硬幣。上述三張影片畫面已讀為 800、1,600、3,200。時間策略僅接受合法起手獎勵翻倍後的值；失敗最多重試兩次，第三次收取當前獎勵以繼續流程，已達 19,800 則继续挑戰。沒有加入 1.1 的新牌面信心門檻，確認按钮沿用 1.0.1 的 0.55 匹配門檻。

## 本片時間量測

固定擷取影片遊戲客戶區（x=517、y=163、寬 1242、高 699），正規化至 1920×1080。140 秒後每 0.5 秒取樣，使用繁中狀態模板辨識並人工檢查關鍵畫面。時間界線有約 0.5 秒取樣誤差；未將前段手動操作／卡住時間算入正常流程。

| 項目 | 樣本 | 平均／採用值 |
|---|---:|---:|
| 未中獎：下注至失敗 | 16 局 | 9.000 秒 |
| 未中獎：下注至下次下注 | 16 局 | 12.125 秒 |
| 失敗畫面至重新下注 | 同上 | 3.125 秒 |
| 中獎：下注至首次翻倍詢問 | 6 局 | 11.500 秒 |
| 無平手：詢問至成功後下次詢問 | 5 次 | 4.700 秒 |
| 決定收手的詢問至下一局下注 | 4 次 | 7.750 秒 |
| 平手額外重抽 | 2 次與正常週期比較 | 約 2.5 秒（近似） |

未中獎完整週期：157.5→172.5、199.5→212、235→247.5、279→291、291→303、303→313.5、313.5→326、326→337、337→348.5、348.5→361.5、361.5→373.5、392→404、404→416、439→452、452→463、463→474.5。

中獎開局：172.5→184.5、212→223、247.5→260.5、373.5→384.5、416→427、474.5→485.5。

普通成功翻倍：223→228、267→271.5、427→431.5、485.5→490、490→495。184.5→191.5、260.5→267 含平手，未混入普通翻倍平均。

收手：149.5→157.5、191.5→199.5、271.5→279、431.5→439。149.5 段包含中途接管，保留於估計但不作整局速度證據。

## 策略與驗證範圍

時間策略比較收取及繼續的預估剩餘達標時間。以 20 代幣為模型格距，現場檢查以實際帳目為準；看不到點數時按分布估计，不視為必勝；平手消耗時間但不翻倍。收取可達 19,800–19,999 時收取；再翻會跨過每日 20,000 時優先保留下一局。已入帳至少 19,800 後取消止盈門檻。極端起手獎金已跨上限時會提前衝刺，因此不保證每天精確落在目標區間。

模型仍沿用 128 組留牌計算的獎金機率先驗；牌堆／記牌假設未由本片全面驗證。模型的中獎開局與未中獎重開成本分開，平手與收手也分開計算。實際按鈕方向與記牌仍由 1.0.1 負責，策略使用副本避免重複扣牌。

離線回歸測試以影片遊戲區裁切為輸入，使用實際狀態辨識與 OCR，攔截所有點擊及帳目寫入。涵蓋原錯誤重現、修正數字、兩種策略發出動作、猜高低、失敗續局、結算續局、辨識失敗回退及達標衝刺。這不能證明真實遊戲一定接收了點擊；完整新版實機測試尚待進行。

執行：`../.venv/Scripts/python.exe -m unittest discover -s tests -v`。

模型比較：`../.venv/Scripts/python.exe benchmark_strategy.py --days 1000 --seed 20260920 --output benchmark_video_calibrated.json`。相同種子與牌序的 1,000 天模擬，舊策略平均 849.97 秒／50.70 局，新策略平均 587.92 秒／34.27 局；達標且未跨上限為 790 與 997 天。這是校準後的模型比較，不是新版實機改善率，尤其仍假設翻倍牌堆獨立、無辨識錯誤、10 次成功後結算（假設的上限）。

發行狀態：v1.2.0-beta.1 預發行測試版；尚未完成新版實機連續運行驗證。

影片及擷取畫面不隨原始碼或執行檔發布。影片回歸測試需要本機的 tests/fixtures；未提供素材時會明確顯示跳過，不能視為通過測試。發布前已在具備素材的本機執行並通過 9 項測試。
